# Home Assistant Windows Client - Windows Dependencies
# Core runtime dependencies (single source of truth: requirements.txt);
# Windows-only packages there carry sys_platform == 'win32' markers.
-r requirements.txt

# Build dependencies
pyinstaller>=6.0.0
